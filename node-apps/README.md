# Apps for the led control server

## available apps:
* clock
* timer
* demo

setup requires node 
then run
```bash
npm install
```

run any of them like this (replace the app name) 
```bash
npm run clock
```

